<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 设置边框的大小和颜色 -->
    <stroke
        android:width="@dimen/cootek_header_padding5"
        android:color="#ffffff" />
    <!-- 设置图形内的颜色,此处为透明色 -->
    <solid android:color="@android:color/transparent" />
    <!-- 定义圆角弧度 圆的话就是半径 -->
    <corners
        android:bottomLeftRadius="@dimen/cootek_header_avatar_radius"
        android:bottomRightRadius="@dimen/cootek_header_avatar_radius"
        android:topLeftRadius="@dimen/cootek_header_avatar_radius"
        android:topRightRadius="@dimen/cootek_header_avatar_radius" />
</shape>